const conceptList = ["gists", "types", "operators", "iteration", "problem solving"];
const join = conceptList.toString();
const seperator = join.replace(/,[s]*g, ", ")
console.log('Today I learned about ${separator}')

//Have a new array that is empty
//Loop through the existing array from start to finish:
///Add each element in the new array 
///come back as a string with a comma separating each string
///print "Today I learned about " and add the string separated with comma

console.log(conceptList);